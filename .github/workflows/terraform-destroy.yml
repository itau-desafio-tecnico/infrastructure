name: Destroy infra AWS terraform

on:
  workflow_dispatch:

jobs:
  terraform:
    name: Destroy infra AWS with Terraform
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout do código
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Setup Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.13.3

      # 3. Configuração das credenciais AWS
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      # 4. Inicialização do Terraform  
      - name: Terraform Init
        working-directory: ./terraform
        run: terraform init -reconfigure

      # 5. Aplicação do Terraform
      - name: Terraform Destroy
        working-directory: ./terraform
        run: |
            terraform destroy -auto-approve -input=false \
                -var "order_service_image"=${{ secrets.ORDER_SERVICE_IMAGE }} \
                -var "requester_service_image"=${{ secrets.REQUESTER_SERVICE_IMAGE }}